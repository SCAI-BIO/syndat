version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.9"
  env:
    - PATH=/home/docs/.local/bin:$PATH
  jobs:
    install:
      - curl -sSL https://install.python-poetry.org | python3 -
      - poetry install --no-root --with docs

sphinx:
  configuration: docs/source/conf.py
